# ${table.tableComment} API 文档

> 模块：`${table.moduleName}` | 业务：`${table.businessName}` | 生成时间：${.now?string('yyyy-MM-dd HH:mm:ss')}

---

## 接口概览

| 方法 | 路径 | 说明 |
|------|------|------|
#foreach ($api in $apis)
| ${api.method} | `${api.path}` | ${api.description} |
#end

---

#foreach ($api in $apis)

## ${api.description}

**请求方式**：`${api.method}`

**请求路径**：`${api.path}`

#if ($api.permission)
**权限标识**：`${api.permission}`
#end

#if ($api.requestParams && $api.requestParams.size() > 0)

### 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
#foreach ($param in $api.requestParams)
| `${param.name}` | ${param.type} | #if ($param.required)✅#else❌#end | ${param.description} |
#end

#end

#if ($api.requestBody)

### 请求体

```json
${api.requestBody}
```

#end

#if ($api.responseParams && $api.responseParams.size() > 0)

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
#foreach ($param in $api.responseParams)
| `${param.name}` | ${param.type} | ${param.description} |
#end

#end

#if ($api.responseExample)

### 响应示例

```json
${api.responseExample}
```

#end

---

#end

## 错误码说明

| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 更新记录

| 日期 | 版本 | 说明 |
|------|------|------|
| ${.now?string('yyyy-MM-dd')} | v1.0.0 | 初始版本 |